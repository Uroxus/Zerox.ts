/**
 * @file
 * Define the states that the bot can run in
 * Used to toggle functionality throughout the project
 */

const enum Environment {
    PROD = "production",
    DEV = "development"
}

export default Environment;